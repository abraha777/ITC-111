// forOf Loop - used to iterate/loop through elements in an array

// Synthax: for(element of array) {do stuff;}

var fruits = ['apple', 'orange', 'pear'];
var e;

for (e of fruits) {
    
    console.log(e);
}